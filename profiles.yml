manufacturing_quality_traceability:
  target: dev
  outputs:
    dev:
      type: duckdb
      path: "{{ env_var('DUCKDB_PATH', 'lakehouse/quality_traceability.duckdb') }}"
      schema: main
      threads: 4
